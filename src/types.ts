// Definição do tipo Contact
export interface Contact {
    id: string;       // ID único para cada contato
    name: string;     // Nome completo do contato
    email: string;    // E-mail do contato
    phone: string;    // Telefone do contato
}
